shared:
    image: node:8

jobs:
    main:
        requires:
            - ~pr
            - ~commit
        environment:
            TESTA: "myenv\nhello"
            TESTB: "${TESTA}2"
        steps:
            - echo: env
            - export: export -p
            - cat: ls /tmp
            - teardown-echo1: echo $TESTA
            - teardown-echo: echo $TESTB
