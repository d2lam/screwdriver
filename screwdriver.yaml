shared:
    image: node:8

jobs:
    main:
        requires:
            - ~pr
            - ~commit
        steps:
            - install: npm install
            - pwd: pwd
            - ls: ls
            - echo: echo done
            
    no-cache:
        requires: [main]
        steps:
            - install-again: npm install
            - echo: echo done
