shared:
    image: node:8
    environment:
        ARTIFACTS_DIR: usertestpath
        TESTA: myenv\nh'el"lo
        TESTB: ${TESTA}world
        BLAH: $ARTIFACTS_DIR/blah
        SECRET: ${MYSECRET}issafe

jobs:
    main:
        requires:
            - ~pr
            - ~commit
        steps:
            - teardownecho: env
            - export: export -p
        secrets:
            - MYSECRET
    second:
        requires: [main]
        steps:
            - teardownecho: env
            - export: export -p
