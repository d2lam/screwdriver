shared:
    image: node:8

jobs:
    main:
        requires:
            - ~pr
            - ~commit
        environment:
            TESTA: "myenv\nhello"
            TESTB: "${TESTA}2"
        steps:
            - echo: env
            - export: export -p
            - teardown-echo: echo $TESTB
